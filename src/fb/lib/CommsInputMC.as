package fb.lib {		import flash.display.MovieClip;	import flash.text.TextField;	import flash.events.*;	import fl.controls.ComboBox;	import com.purplemedia.*;	import com.purplemedia.vo.*;	import com.purplemedia.models.*;	import com.purplemedia.mains.*;	import fl.data.DataProvider;		public class CommsInputMC extends MovieClip{				private var model:ModelLocator = ModelLocator.getInstance();						public function CommsInputMC(){								}			/*positionCombo.addEventListener(Event.CHANGE, onPositionSelected)		private  function onPositionSelected(event:Event):void		{			var item:Object = positionCombo.selectedItem;			trace("onPositionSelected: "+item.data);			model.contactvo.contactPosition = item.data;			//setContact(model.contactvo);			CustDetails.updateDetails("contactPosition", item.data)		}				private function setPositionsDataProvider(value:Array,selItem:Object=null):void		{			trace("setPositionsDataProvider:  "+value.contactPosition);			var cdp:DataProvider = new DataProvider(value);			var co:Object = {label:"Select...",data:""};			cdp.addItemAt(co,0);						positionCombo.dataProvider = cdp;			positionCombo.sortItemsOn("label", Array.CASEINSENSITIVE);			if(selItem==null){				positionCombo.selectedItem = co;			}else{				positionCombo.selectedItem = selItem;			}		}*/	}}